//Login Employee offscreen
describe("Workplace list", () => {
  it.skip("Shows at least one workplace", () => {
    expect.hasAssertions();
  });
  it.skip("Shows message on no workplaces", () => {
    expect.hasAssertions();
  });
});
