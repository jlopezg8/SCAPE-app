/*require('dotenv').config();

export default function requireEnvVar(name: string) {
  const value = process.env[name];
  if (value === undefined) {
    throw new Error(`${name} not defined in the .env file`);
  }
  return value;
}*/
